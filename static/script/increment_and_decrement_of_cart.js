function decrement_buttons( )
{
document.getElementById('cart').innerHTML=
var user_input = parseFloat( quantity.value );
currentValue = user_input;
currentValue = parseFloat( quantity.value ) || user_input;
quantity.value = ( currentValue -1);

}
function increment_buttons( )
{
document.getElementById('cart').innerHTML=
var user_input = parseFloat( quantity.value );
currentValue = user_input;
currentValue = parseFloat( quantity.value ) || user_input;
quantity.value = ( currentValue +1);

}